import VirtualNode from "../../core/node.js";

class Map extends VirtualNode {
    constructor(matrix) {
        super({
            attrs: {
                class: "map",
            },
            
        })
    }
}